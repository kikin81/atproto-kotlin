{
  "lexicon": 1,
  "id": "example.split",
  "description": "Contextual split: `#shared` has optional fields and is reached from BOTH mutation (procedure input) and read (procedure output) via example.sendShared. Should emit Shared (read-side, T? = null) + SharedInput (mutation-side, AtField<T> = Missing).",
  "defs": {
    "shared": {
      "type": "object",
      "required": ["id"],
      "properties": {
        "id": { "type": "string", "format": "did" },
        "note": { "type": "string" },
        "score": { "type": "integer" }
      }
    }
  }
}
