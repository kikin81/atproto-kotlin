{
  "lexicon": 1,
  "id": "example.strongRef",
  "description": "Shared no-split case. Mimics com.atproto.repo.strongRef: all fields required, used from both mutation and read contexts → emits as a single class, no Input variant.",
  "defs": {
    "main": {
      "type": "object",
      "required": ["uri", "cid"],
      "properties": {
        "uri": { "type": "string", "format": "at-uri" },
        "cid": { "type": "string", "format": "cid" }
      }
    }
  }
}
