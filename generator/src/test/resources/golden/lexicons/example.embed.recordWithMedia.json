{
  "lexicon": 1,
  "id": "example.embed.recordWithMedia",
  "description": "Other half of the mutual recursion pair. Its `#view.record` refs `example.embed.record#view`, which in turn refs back to `example.embed.recordWithMedia#view` via the union in viewRecord.embeds[].",
  "defs": {
    "view": {
      "type": "object",
      "required": ["record", "media"],
      "properties": {
        "record": { "type": "ref", "ref": "example.embed.record#view" },
        "media": { "type": "ref", "ref": "example.embed.images#view" }
      }
    }
  }
}
